# CEFET-MG • Dashboard de Pesquisa (Streamlit)

Rodar local:

```
pip install -r requirements.txt
streamlit run streamlit_app.py
```

Excel padrão: `data/dados_cefet.xlsx`. Para arquivos grandes, use Git LFS.
